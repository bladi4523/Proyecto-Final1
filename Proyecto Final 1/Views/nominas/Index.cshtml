@model IEnumerable<Proyecto_Final_1.Models.nomina>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Año)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Mes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Monto_Total)
        </th>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Año)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Monto_Total)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Nom }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Nom }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Nom })
        </td>
    </tr>
}

</table>
